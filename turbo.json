{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["node_modules", "dist"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["node_modules", "dist"]
    },
    "clean": {}
  }
}
